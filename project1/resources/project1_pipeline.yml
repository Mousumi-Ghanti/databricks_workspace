# The main pipeline for project1
resources:
  pipelines:
    project1_pipeline:
      name: project1_pipeline
      target: project1_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
